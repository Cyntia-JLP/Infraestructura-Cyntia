# /etc/fail2ban/jail.local — cyntia (nodo principal)
# Configuración de fail2ban para protección SSH
# Backend systemd necesario en Debian 12 (no usa /var/log/auth.log)

[DEFAULT]
bantime = 1h
findtime = 10m
maxretry = 5

[sshd]
enabled = true
port = ssh
backend = systemd
maxretry = 3
